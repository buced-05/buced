# Configuration pour VPS Production
# Copiez ce fichier vers backend/.env et modifiez les valeurs

# Django
SECRET_KEY=change-me-to-a-secure-random-key-min-50-characters
DJANGO_DEBUG=0
# Liste des domaines autorisés séparés par des virgules
DJANGO_ALLOWED_HOSTS=foundation.newtiv.com,91.108.120.78,localhost,127.0.0.1

# CORS - Origines autorisées séparées par des virgules
CORS_ALLOWED_ORIGINS=http://foundation.newtiv.com,https://foundation.newtiv.com,http://91.108.120.78,https://91.108.120.78

# SSL/HTTPS (activer seulement si SSL est configuré)
SECURE_SSL_REDIRECT=0

# Base de données PostgreSQL (pour production)
POSTGRES_DB=buced_db
POSTGRES_USER=buced_user
POSTGRES_PASSWORD=change-me-to-secure-password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis / Celery (optionnel)
REDIS_URL=redis://localhost:6379/0

# Logging
DJANGO_LOG_LEVEL=info

